# CONSTITUICAO DO CEP_BUNDLE_CORE

## Principios

1. O repositorio deve executar com o interpretador oficial definido em `/home/wilson/PortfolioZero/.venv/bin/python`.
2. Toda execucao de bootstrap deve gerar evidencias autocontidas em `outputs/`.
3. Gates de ambiente e rastreabilidade por hash sao obrigatorios.

## Emendas

As emendas oficiais devem ser registradas em `docs/emendas/`.

